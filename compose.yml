name: campus-seekr

services:
  app:
    build: .
    environment:
      DATABASE_URL: "postgresql+psycopg2://seekr:seekr-devonly@db:5432/seekr"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=2)"]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s
    depends_on:
      db:
        condition: service_healthy

  gateway:
    image: nginx:1.27.4-alpine
    ports: ["8000:80"]
    volumes: ["./gateway.conf:/etc/nginx/conf.d/default.conf:ro"]
    depends_on: [app]

  db:
    image: postgres:16.6-alpine3.20
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: seekr
      POSTGRES_PASSWORD: seekr-devonly
      POSTGRES_DB: seekr
    volumes:
      - seekr_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U seekr -d seekr"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s

volumes:
  seekr_data: